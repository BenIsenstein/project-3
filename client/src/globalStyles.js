import { createGlobalStyle } from 'styled-components/macro'

export default createGlobalStyle`
    body {
        
    }

    h1, h2, h3, h4, h5, h6 {
        margin: 1em;
        color: #2e2e2e;
    }

    p {
        margin: 0.8em 0;
        font-size: 1em;
        color: #616161;
    }

    button {
        cursor: pointer;
    }
`